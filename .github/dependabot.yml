# Weekly dependency-update scanning: the two pinned dev deps and the
# workflow actions. Zero runtime deps by design — that never changes here.
version: 2
updates:
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
